{% extends "base.html.twig" %}


  {% block body %}
  <!-- ======= Book A Table Section ======= -->
    <section id="book-a-table" class="book-a-table">
      <div class="container" data-aos="fade-up">

        <div class="section-title">
          <h2>Reservation</h2>
          <p>Reserver une table</p>
        </div>

        
     <div class="card-body " style="color: #cda45e;">
        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
          {{ form_errors(form.date) }}
          {{ form_errors(form.nb_personnes) }}
       
        <div class="form-group">
          {{ form_label(form.date,"Date") }}
          {{ form_widget(form.date)}}
        </div>
        <div class="form-group">
          {{ form_label(form.nb_personnes,"nbre de personnes") }}
          {{ form_widget(form.nb_personnes)}}
        </div>
        
        {{form_rest(form)}}
        {{ form_end(form) }}
                
                <img src="{{ qr_code_url('http://127.0.0.1:8000/listCoinsf#events') }}" />
</div>

                
    </div>
      
      </div>
    </section><!-- End Book A Table Section -->



  {% endblock %}
  