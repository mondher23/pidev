<form method="post" action="{{ path('cartefidelite_delete', {'id': cartefidelite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cartefidelite.id) }}">
     <button <a class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
